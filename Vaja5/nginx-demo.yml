apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-yaml-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-yaml-demo
  template:
    metadata:
      labels:
        app: nginx-yaml-demo
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-yaml-demo
spec:
  type: NodePort
  selector:
    app: nginx-yaml-demo
  ports:
  - port: 80
    targetPort: 80